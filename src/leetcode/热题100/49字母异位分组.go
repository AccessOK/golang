package leetcode

import (
	"sort"
	"strings"
)

func groupAnagrams(strs []string) [][]string {
	mark := map[string]int{}
	strs1 = 
	for i := 0; i < len(strs1); i++ {
		sort.Sort(sort.StringSlice(strs1[i]))

	}
}
