# 编译与运行
- go build：先编译生成可执行文件，可将该可执行文件拷贝到没有go开发环境的机器上运行。
- go run :直接编译运行，运行时间比go build 长
- 程序执行入口：main函数
- 严格区分大小写
- 语句结尾时不用加分号，但是一行如果需要写多条语句的时候需要添加分号。
- 声明的变量和引入的软件包必须使用
- gofmt 展示正确缩进的格式  -w 写入文件
- 一行不超过80个字符

# 基本变量与类型
- 数据类型
    - 基本数据类型
        - 数值型 
            - 整数类型 : int(系统字节数默认值决定，不声明类型时会默认判断为int)，int8(-128-127)，int16，uint(系统字节数默认值决定)，uint8(0-255)，uint16,uint32,uint64,byte
            - 浮点类型 : float32,float64,浮点类型底层存储：符号位+指数位+尾数位，所以尾数位只是存了大概，可能有精度损失。
        - 字符型 : 没有单独的字符型，使用byte保存单个字母字符
        - 布尔型
        - 字符串
    - 派生数据类型/复杂数据类型
        - 指针
        - 数组
        - 结构体
        - 管道
        - 函数
        - 切片
        - 接口
        - map
# 进程，线程，协程
协程(goroutine):
    - 微线程，用户态的轻量级线程。
    - 执行A函数的时候，可以随时中断，去执行B函数。
    - 对于单线程下